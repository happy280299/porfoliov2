import styled from "styled-components";

export const KeyvisualContainer = styled.section`
  padding-top: 100px;
  position: relative;
  z-index: 1;
  pointer-events: none;
  .wrapper-inner {
    position: relative;
    width: 300px;
    margin: 0 auto 68px;
  }
`;
